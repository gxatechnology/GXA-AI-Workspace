import React, { useEffect, useRef } from 'react';
import { X } from 'lucide-react';
export default function PlanLimitDialog({ open, onClose, onUpgrade }: { open: boolean; onClose: () => void; onUpgrade: () => void }) { const button = useRef<HTMLButtonElement>(null); useEffect(() => { if (!open) return; button.current?.focus(); const key = (event: KeyboardEvent) => { if (event.key === 'Escape') onClose(); }; document.addEventListener('keydown', key); return () => document.removeEventListener('keydown', key); }, [open, onClose]); if (!open) return null; return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/55 p-4" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget) onClose(); }}><section role="dialog" aria-modal="true" aria-labelledby="plan-limit-title" className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-6 shadow-2xl dark:border-zinc-700 dark:bg-zinc-900"><button onClick={onClose} className="float-right rounded-lg p-2 text-slate-400 hover:bg-slate-100 dark:hover:bg-zinc-800" aria-label="Close"><X className="h-4 w-4" /></button><p className="text-xs font-bold uppercase tracking-widest text-teal-600">Plan limit reached</p><h2 id="plan-limit-title" className="mt-2 text-xl font-black">You have reached the limit of your current plan.</h2><p className="mt-2 text-sm text-slate-500">Your work is safe. Upgrade to continue with higher limits.</p><div className="mt-6 flex justify-end gap-2"><button onClick={onClose} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-bold dark:border-zinc-700">Not now</button><button ref={button} onClick={onUpgrade} className="theme-primary-action rounded-xl px-4 py-2.5 text-sm font-black">Upgrade to ₹149</button></div></section></div>; }
